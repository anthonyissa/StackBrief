{
  "name": "stackbrief",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/engine.ts",
    "start:once": "bun run src/run-once.ts",
    "dev": "bun run src/index.ts",
    "test": "bun run src/test.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "openai": "^4.104.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.3.0"
  }
}
